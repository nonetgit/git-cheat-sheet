[![Git](https://img.shields.io/badge/Git-CheatSheet-red?style=for-the-badge&logo=git)](https://git-scm.com/)
[![GitHub](https://img.shields.io/badge/GitHub-2026-blue?style=for-the-badge&logo=github)](https://github.com)
[![Version](https://img.shields.io/badge/version-3.0-green?style=for-the-badge)]()
[![Author](https://img.shields.io/badge/author-nonetgit-purple?style=for-the-badge)](https://github.com/nonetgit)

# 📚 Git Cheat Sheet (Шпаргалка) 2026

> **GitHub Edition** | Актуально для Git 2.40+ и 2026 года
> Сохрани в закладки, чтобы не потерять. Все основные команды Git в одном месте.
> 
> 👨‍💻 Автор: **nonetgit**

---

## 📋 Оглавление

- [🚀 Быстрый старт (GitHub)](#-быстрый-старт-github)
- [🔧 Настройка и конфигурация](#-настройка-и-конфигурация)
- [📁 Создание и клонирование](#-создание-и-клонирование)
- [📝 Staging и коммиты](#-staging-и-коммиты)
- [🌿 Ветвление и слияние](#-ветвление-и-слияние)
- [🌐 Удалённые репозитории (GitHub)](#-удалённые-репозитории-github)
- [🔍 Просмотр истории](#-просмотр-истории)
- [↩️ Отмена изменений](#️-отмена-изменений)
- [🔖 Теги и релизы](#-теги-и-релизы)
- [🔐 Безопасность (SSH и секреты)](#-безопасность-ssh-и-секреты)
- [🤖 GitHub CLI (gh)](#-github-cli-gh)
- [🧹 Обслуживание репозитория](#-обслуживание-репозитория)
- [📦 Git LFS (Большие файлы)](#-git-lfs-большие-файлы)
- [🎯 Популярные Workflow](#-популярные-workflow-2026)
- [⚡ Алиасы и продуктивность](#-алиасы-и-продуктивность)
- [🚨 Опасные команды](#-опасные-команды)
- [🔗 Полезные ссылки](#-полезные-ссылки)

---

## 🚀 Быстрый старт (GitHub)

```bash
# 1. Клонировать репозиторий
git clone git@github.com:nonetgit/repo.git

# 2. Создать ветку для новой задачи
git switch -c feature/awesome-feature

# 3. Внести изменения и закоммитить
git add .
git commit -m "feat: add awesome feature"

# 4. Отправить на GitHub и создать Pull Request
git push -u origin feature/awesome-feature
# Далее открыть ссылку, которую выдаст Git, или использовать gh cli:
gh pr create --fill
```

[⬆️ К оглавлению](#-оглавление)

---

## 🔧 Настройка и конфигурация

Современные настройки (2026): поддержка новых алгоритмов и улучшение безопасности.

| Команда | Описание |
|---------|----------|
| `git config --global user.name "nonetgit"` | Имя для подписи коммитов |
| `git config --global user.email "nonetgit@example.com"` | Email (должен совпадать с GitHub) |
| `git config --global init.defaultBranch main` | Ветка по умолчанию теперь **main** |
| `git config --global core.editor "code --wait"` | Редактор (VS Code) |
| `git config --global pull.rebase true` | `git pull` всегда делает rebase |
| `git config --global fetch.prune true` | Автоматически чистить удаленные ветки |
| `git config --global core.autocrlf input` | (macOS/Linux) Настройка окончаний строк |
| `git config --global credential.helper store` | Сохранять пароль (небезопасно, лучше SSH) |
| `git config --global --list` | Просмотр всех настроек |

[⬆️ К оглавлению](#-оглавление)

---

## 📁 Создание и клонирование

| Команда | Описание |
|---------|----------|
| `git init` | Создать новый локальный репозиторий |
| `git clone git@github.com:nonetgit/repo.git` | Клонировать по SSH (рекомендуется) |
| `git clone https://github.com/nonetgit/repo.git` | Клонировать по HTTPS |
| `git clone --depth 1 <url>` | Поверхностное клонирование (без истории) |
| `git clone --branch main --single-branch <url>` | Клонировать только одну ветку |
| `gh repo clone nonetgit/repo` | Клонировать через GitHub CLI |

[⬆️ К оглавлению](#-оглавление)

---

## 📝 Staging и коммиты

Современный синтаксис (заменили `checkout` на `restore/switch`).

| Команда | Описание |
|---------|----------|
| `git status` | Показать статус |
| `git add .` | Добавить все изменения |
| `git add -p` | Добавить интерактивно (частями) |
| `git commit -m "feat: message"` | Коммит с сообщением |
| `git commit --amend` | Исправить последний коммит (не пушить, если уже на GitHub!) |
| `git commit --amend --no-edit` | Добавить забытые файлы в последний коммит |
| `git restore <file>` | Отменить изменения в файле |
| `git restore --staged <file>` | Убрать файл из staged (unstage) |
| `git rm <file>` | Удалить файл и зафиксировать удаление |

[⬆️ К оглавлению](#-оглавление)

---

## 🌿 Ветвление и слияние

| Команда | Описание |
|---------|----------|
| `git branch` | Список локальных веток |
| `git branch -a` | Список всех веток (включая удаленные) |
| `git switch -c <name>` | Создать и переключиться (новый стандарт) |
| `git switch <name>` | Переключиться на ветку |
| `git branch -d <name>` | Удалить ветку (есть слита) |
| `git branch -D <name>` | Удалить ветку принудительно |
| `git merge --no-ff <branch>` | Слить ветку, создав коммит слияния |
| `git rebase -i HEAD~3` | Интерактивный rebase (изменить историю) |
| `git cherry-pick <commit-hash>` | Взять один коммит из другой ветки |

[⬆️ К оглавлению](#-оглавление)

---

## 🌐 Удалённые репозитории (GitHub)

| Команда | Описание |
|---------|----------|
| `git remote -v` | Посмотреть привязанные репозитории |
| `git remote add origin git@github.com:nonetgit/repo.git` | Добавить remote (SSH) |
| `git push -u origin main` | Отправить ветку на GitHub (первый раз) |
| `git push` | Отправить изменения (если upstream задан) |
| `git push --force-with-lease` | **Безопасный** force push (рекомендуется) |
| `git push --force` | Опасный force push (только для личных веток) |
| `git fetch --prune` | Забрать обновления и удалить мертвые ссылки |
| `git pull --rebase` | Стянуть изменения и перебазировать свои |
| `git push origin --delete <branch>` | Удалить ветку на GitHub |

[⬆️ К оглавлению](#-оглавление)

---

## 🔍 Просмотр истории

| Команда | Описание |
|---------|----------|
| `git log --oneline` | Краткая история |
| `git log --oneline --graph --all` | Визуализация всех веток |
| `git log -p <file>` | Показать историю изменений файла (патчи) |
| `git diff` | Что изменилось, но не в staged |
| `git diff --staged` | Что в staged |
| `git diff main..feature` | Сравнить ветки |
| `git blame <file>` | Кто написал эту строку? |
| `git reflog` | **Журнал всех движений** (спасение после reset) |
| `git shortlog -sn` | Статистика коммитов по авторам |

[⬆️ К оглавлению](#-оглавление)

---

## ↩️ Отмена изменений

| Команда | Описание |
|---------|----------|
| `git reset --soft HEAD~1` | Отменить коммит, изменения в staged |
| `git reset --mixed HEAD~1` | Отменить коммит, изменения в working dir |
| `git reset --hard HEAD~1` | **Опасно!** Удалить коммит и изменения |
| `git revert <commit>` | **Безопасно!** Создать новый коммит, отменяющий старый |
| `git clean -n` | Показать, что будет удалено |
| `git clean -fd` | Удалить неотслеживаемые файлы и папки |

[⬆️ К оглавлению](#-оглавление)

---

## 🔖 Теги и релизы

| Команда | Описание |
|---------|----------|
| `git tag` | Показать теги |
| `git tag -a v1.0.0 -m "Release 1.0.0 by nonetgit"` | Создать аннотированный тег |
| `git push origin v1.0.0` | Отправить тег на GitHub |
| `git push --tags` | Отправить все теги |
| `git tag -d v1.0.0` | Удалить локальный тег |
| `git push origin --delete v1.0.0` | Удалить тег на GitHub |
| `gh release create v1.0.0 --generate-notes` | Создать релиз на GitHub (CLI) |

[⬆️ К оглавлению](#-оглавление)

---

## 🔐 Безопасность (SSH и секреты)

*Рекомендации 2026: Используй SSH-ключи с алгоритмом Ed25519.*

```bash
# Генерация SSH-ключа (современный стандарт)
ssh-keygen -t ed25519 -C "nonetgit@github.com"

# Добавление ключа в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Добавить ключ в аккаунт GitHub (Settings -> SSH and GPG keys)
cat ~/.ssh/id_ed25519.pub
```

**Секреты и безопасность данных:**
| Команда | Описание |
|---------|----------|
| `git update-index --skip-worktree <file>` | Игнорировать локальные изменения в файле |
| `git update-index --assume-unchanged <file>` | Считать файл неизменным |
| `git rm --cached <file>` | Убрать из Git, но оставить локально (для секретов) |
| `git secrets --install` | Установка плагина для защиты от утечек |
| `git filter-repo --path secrets.txt --invert-paths` | **Новый стандарт** для удаления файлов из истории (замена `filter-branch`) |

> ⚠️ **nonetgit советует:** Никогда не коммить файлы с паролями. Используй `git-crypt` или `sops`.

[⬆️ К оглавлению](#-оглавление)

---

## 🤖 GitHub CLI (gh)

Современная разработка невозможна без GitHub CLI.

| Команда | Описание |
|---------|----------|
| `gh auth login` | Войти в GitHub из консоли |
| `gh repo create my-repo --public` | Создать репозиторий на GitHub |
| `gh repo clone nonetgit/repo` | Клонировать репозиторий |
| `gh pr create --fill` | Создать Pull Request из текущей ветки |
| `gh pr checkout 123` | Переключиться на PR #123 |
| `gh pr list` | Список открытых PR |
| `gh pr review --approve` | Одобрить PR |
| `gh issue list` | Список задач |
| `gh release create v1.0.0` | Создать релиз |
| `gh gist create file.txt` | Создать Gist |

[⬆️ К оглавлению](#-оглавление)

---

## 🧹 Обслуживание репозитория

| Команда | Описание |
|---------|----------|
| `git fsck` | Проверка целостности |
| `git gc` | Сборка мусора (оптимизация) |
| `git gc --aggressive --prune=now` | Агрессивная оптимизация |
| `git worktree list` | Список рабочих деревьев |
| `git worktree add ../hotfix hotfix` | Создать новую рабочую копию для ветки |

[⬆️ К оглавлению](#-оглавление)

---

## 📦 Git LFS (Большие файлы)

Для работы с PSD, видео, бинарными файлами на GitHub.

```bash
# Установка LFS
git lfs install

# Отслеживать файлы .psd
git lfs track "*.psd"

# Добавить правила в .gitattributes
git add .gitattributes

# Работаем как обычно
git add file.psd
git commit -m "Add design file"
git push
```

| Команда | Описание |
|---------|----------|
| `git lfs track "*.mp4"` | Начать отслеживать MP4 через LFS |
| `git lfs ls-files` | Список файлов в LFS |
| `git lfs status` | Статус LFS |
| `git lfs migrate import` | Конвертировать существующие файлы в LFS |

[⬆️ К оглавлению](#-оглавление)

---

## 🎯 Популярные Workflow 2026

### 🔁 GitHub Flow (Стандарт для большинства команд)
```bash
git switch main
git pull --rebase origin main
git switch -c feature/issue-123
# Работаем...
git add .
git commit -m "feat: close #123"
git push -u origin feature/issue-123
# gh pr create --fill  (или открыть ссылку)
```

### 🏷️ GitFlow (Сложный, для релизных циклов)
```bash
# Начать фичу
git flow feature start user-auth
# Закончить фичу (авто-слияние в develop)
git flow feature finish user-auth

# Начать релиз
git flow release start 1.2.0
git flow release finish 1.2.0  # Сливает в main и создает тег
```

### 🔄 Работа с Open Source (Fork)
```bash
# 1. Форкнуть на GitHub
# 2. Клонировать свой форк
git clone git@github.com:nonetgit/repo.git
# 3. Добавить оригинал как upstream
git remote add upstream git@github.com:original/repo.git
# 4. Синхронизация
git fetch upstream
git rebase upstream/main
git push -f  # В свой форк
```

[⬆️ К оглавлению](#-оглавление)

---

## ⚡ Алиасы и продуктивность

Добавь в `~/.gitconfig`:

```ini
[alias]
    st = status -sb
    co = switch
    ci = commit -v
    br = branch -a
    lg = log --oneline --graph --all --decorate
    last = log -1 HEAD
    unstage = restore --staged .
    who = shortlog -sn --
    undo = reset --soft HEAD~1
    amend = commit --amend --no-edit
    pr = "!gh pr create --fill"
    nonet = "!git log --author=nonetgit --oneline --graph"  # Специально для nonetgit
```

**Использование:**
- `git lg` — красивый граф
- `git st` — статус
- `git undo` — откатить последний коммит (оставив изменения)
- `git pr` — создать Pull Request (через gh)
- `git nonet` — показать историю коммитов автора nonetgit

[⬆️ К оглавлению](#-оглавление)

---

## 🚨 Опасные команды

| Команда | Риск | Альтернатива |
|---------|------|--------------|
| `git reset --hard HEAD~1` | 🚫 Потеря изменений | `git reset --soft` или `git revert` |
| `git push --force` | 🚫 Перезапись истории на сервере | `git push --force-with-lease` |
| `git clean -fd` | 🚫 Удаление всех новых файлов | `git clean -n` (проверить сначала) |
| `git rebase main` на публичной ветке | 🚫 Ад для коллег | Никогда так не делай |

[⬆️ К оглавлению](#-оглавление)

---

## 🔗 Полезные ссылки

- 📖 [Официальная документация Git](https://git-scm.com/doc)
- 🎓 [GitHub Skills](https://skills.github.com/) — бесплатные курсы
- 🕹️ [Learn Git Branching](https://learngitbranching.js.org/?locale=ru_RU) — игра
- 📺 [Git для начинающих (2025)](https://www.youtube.com/results?search_query=git+2025+%D1%83%D1%80%D0%BE%D0%BA) — свежие туториалы
- 🔧 [GitHub CLI Manual](https://cli.github.com/manual/)
- 🐙 [Профиль nonetgit на GitHub](https://github.com/nonetgit)

[⬆️ К оглавлению](#-оглавление)

---

## 📄 Лицензия

Свободное использование и распространение. Автор: **nonetgit** (обновлено в 2026).

---

⭐ **Поставь звезду, если шпаргалка была полезна!**
